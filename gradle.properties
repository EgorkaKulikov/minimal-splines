# Publication coordinates; the version can be overridden with the -Pversion property.
group=io.github.egorkakulikov
version=1.1.0
# Version of the numerical-core library that minimal-splines is built against.
numericalCoreVersion=1.1.0
org.gradle.jvmargs=-Xmx2g
