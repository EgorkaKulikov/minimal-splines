# CLAUDE.md

Все правила работы в этом репозитории — в [`AGENTS.md`](AGENTS.md). Прочитай его
перед изменением файлов; здесь второй копии правил нет.

Кратко: библиотека минимальных сплайнов; импортировать можно только `numerics.*`
(артефакт `numerical-core`); ничего об интегральных уравнениях; после изменения формул —
`./gradlew publishToMavenLocal` и характеризационные гейты в `integral-equations`.
