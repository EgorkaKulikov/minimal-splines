// Отдельная Gradle-сборка: НЕ включена в корневой settings.gradle.kts библиотеки намеренно.
// Пример подключает библиотеки ТОЛЬКО как опубликованные артефакты — ровно так, как это
// сделает независимый «четвёртый» проект.
rootProject.name = "standalone-consumer"
